spring:
  application:
    name: premierProjet

  datasource:
    url: jdbc:postgresql://localhost:5432/ludotheque
    username: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        default_schema: public
        format_sql: true